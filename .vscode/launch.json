{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "aws-sam",
      "request": "direct-invoke",
      "name": "sam-app:hello-world (nodejs22.x)",
      "invokeTarget": {
        "target": "template",
        "templatePath": "${workspaceFolder}/sam-app/template.yaml",
        "logicalId": "HelloWorldFunction"
      },
      "lambda": {
        "payload": {
          "path": "${workspaceFolder}/sam-app/events/event.json"
        },
        "runtime": "nodejs22.x"
      }
    }
  ]
}
