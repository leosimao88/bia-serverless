version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
  build:
    commands:
      - sam build
      - sam deploy --no-confirm-changeset --no-fail-on-empty-changeset --stack-name bia-serverless --region us-east-1 --capabilities CAPABILITY_IAM --resolve-s3
