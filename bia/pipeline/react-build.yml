version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 22
  build:
    commands:
      - echo "Iniciando build do react..."
      - cd bia/client
      - npm install
      - NODE_OPTIONS=--openssl-legacy-provider VITE_API_URL=https://iz1zqkucc9.execute-api.us-east-1.amazonaws.com/Prod SKIP_PREFLIGHT_CHECK=true npm run build
artifacts:
  files:
    - '**/*'
  base-directory: bia/client/build

  